import React from "react";
import { HiMenuAlt2 } from "react-icons/hi";

const SidebarButton = ({ open }) => {
    return <HiMenuAlt2 size={20} className="cursor-pointer origin-right" />;
};

export default SidebarButton;
